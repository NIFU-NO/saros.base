# Convert mesos_groups to internal data frame format

Convert mesos_groups to internal data frame format

## Usage

``` r
convert_mesos_groups_to_df(mesos_groups)
```

## Arguments

- mesos_groups:

  Named list or data frame

## Value

List of single-column data frames (internal format)
