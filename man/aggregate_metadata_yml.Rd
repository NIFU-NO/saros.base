% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregate_metadata_yml.R
\name{aggregate_metadata_yml}
\alias{aggregate_metadata_yml}
\title{Aggregate the \verb{_metadata.yml} Inheritance Chain}
\usage{
aggregate_metadata_yml(path = ".")
}
\arguments{
\item{path}{\emph{Folder to resolve the chain for}

\verb{scalar<character>} // \emph{default:} \code{"."} (\code{optional})

The folder to treat as the deepest level of the chain, normally the one
holding the \code{.qmd} being rendered. The default is correct at render time:
Quarto executes a document with the working directory set to that
document's own folder unless the project sets \code{execute-dir: project}.}
}
\value{
A named \code{list} of the merged YAML, empty if nothing was found. The
\code{params} element is the part generated chapters use.
}
\description{
Collects every \verb{_metadata.yml} from the Quarto project root down to \code{path}
and merges them, so that a deeper folder overrides a shallower one. This is
the inheritance chain \code{\link[=setup_mesos]{setup_mesos()}} writes — project, then wave, then
chapter — and the mechanism behind the \code{parameters} object that generated
chapters use.

Quarto's own \code{params} cannot provide this: it resolves to the individual
\code{.qmd} file's YAML and carries no inheritance.
}
\details{
The walk is bounded by the Quarto project root — the nearest ancestor of
\code{path} holding a \verb{_quarto.yml} or a \verb{_quarto.yaml}. A \verb{_metadata.yml} above
that root is not read. When no project file is found anywhere above \code{path},
only \code{path} itself is read, so the walk can never escape into unrelated
folders.

A folder without a \verb{_metadata.yml} does not end the walk; it simply
contributes nothing. Neither does an empty or non-mapping one, which is what
\code{\link[=setup_mesos]{setup_mesos()}} writes at intermediate levels.

Both \verb{_metadata.yml} and \verb{_metadata.yaml} are read. In the unlikely event
that one folder holds both, \verb{_metadata.yml} is merged first, so
\verb{_metadata.yaml} wins.

Merging is \code{\link[utils:modifyList]{utils::modifyList()}}, which descends into nested lists: a deeper
folder overriding \code{params$mesos_group} leaves its siblings under \code{params}
intact.
}
\examples{
project <- fs::path(tempdir(), "example_project")
fs::dir_create(fs::path(project, "Chapter"))
cat("project:\n  type: website\n", file = fs::path(project, "_quarto.yml"))
yaml::write_yaml(
  list(params = list(save = TRUE, wave = "2025")),
  file = fs::path(project, "_metadata.yml")
)
yaml::write_yaml(
  list(params = list(wave = "2026")),
  file = fs::path(project, "Chapter", "_metadata.yml")
)

# The deeper `wave` wins; `save` is inherited from the project level.
aggregate_metadata_yml(fs::path(project, "Chapter"))$params
}
